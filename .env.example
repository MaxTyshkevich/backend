PORT=3000
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
# Current shop uses in-memory storage; DATABASE_URL is not needed to start it.
# Prisma is reserved for a future persistent storage migration.
